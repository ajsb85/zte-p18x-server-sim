// Global setup for Jest tests (e.g., mock global objects, etc.)
// For example:
// process.env.NODE_ENV = 'test';

// You can also extend Jest matchers here if needed
// require('@testing-library/jest-dom/extend-expect'); // if using jest-dom

// Silence console.log during tests if desired
// global.console = {
//   ...console,
//   log: jest.fn(),
//   debug: jest.fn(),
//   info: jest.fn(),
//   warn: jest.fn(),
//   error: jest.fn(),
// };
